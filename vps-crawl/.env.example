# Chép thành .env rồi điền. KHÔNG commit .env.
SUPABASE_URL=https://vklsqexomaconbzoufmu.supabase.co
SUPABASE_KEY=sb_publishable_YDj6liDtYbMFq38GhwCwIQ_KlS-lNwI

# Khoá chung giữa admin.html (CRAWL_API_KEY) và service này. Đặt chuỗi ngẫu nhiên dài,
# ví dụ sinh bằng:  python -c "import secrets;print(secrets.token_urlsafe(24))"
CRAWL_API_KEY=doi-thanh-chuoi-ngau-nhien-dai

# Domain Cloudflare Pages có sẵn Pages Function /api/extract-lesson
PAGES_BASE=https://cungnhau.pages.dev

# Các origin được phép gọi (CORS) — nơi admin.html chạy
ALLOW_ORIGINS=https://cungnhau.pages.dev,https://johnong55.github.io

# Cắt nội dung tối đa gửi cho AI (ký tự)
MAX_CHARS=12000
