# CLAUDE.md — "cùng nhau" (BeTogether)

> File này Claude Code tự đọc mỗi session. Đọc kỹ trước khi sửa. Người dùng nói **tiếng Việt**, thích tông ấm/dễ thương, emoji, pastel. Trả lời tiếng Việt.

## App là gì
Web nhỏ, riêng tư cho **một cặp đôi** (user & người yêu) học từ vựng và gắn kết. Tên hiển thị: **"cùng nhau"**. Repo GitHub: **BeTogether**.

Tính năng: đăng nhập + ghép đôi 2 người → học từ vựng (chủ đề B2 có sẵn + bộ tự tạo) bằng **thẻ lật** và **trắc nghiệm/trộn chữ** → gửi "bài dò" cho nhau → **giữ chuỗi 🔥** → **đấu trường** thách đấu trực tiếp có cược → **ghi chú/nhắc nhở có ngày** → **câu hỏi mỗi ngày** → **đếm ngày bên nhau**. Cài được như **app (PWA)**. Có **animation Lottie** (tim loading, vương miện thắng, mưa thua).

## Kiến trúc (QUAN TRỌNG)
- **Toàn bộ app nằm trong 1 file `index.html`** (~1360 dòng: CSS + JS thuần, không framework, không build). Render bằng `render()` set `innerHTML`, không có virtual DOM.
- **Backend = Supabase** (gọi trực tiếp từ trình duyệt qua `@supabase/supabase-js@2` CDN). KHÔNG có server Node riêng (user từng nhờ tách backend/frontend rồi ĐỔI Ý — đã xoá; đừng dựng lại).
- **`library.js`**: `window.LIBRARY` = 28 chủ đề, ASCII \u-escaped, trích từ PDF. Topic 1–14 = TỪ VỰNG B2 (~780 từ đơn, pdfplumber); topic 15–28 có `kind:'colloc'` = COLLOCATION TOEIC (500 cụm, PDF Benzen là ảnh scan → đã render ảnh + đọc bằng mắt). File tĩnh, nhúng sẵn (không nằm trong Supabase). Tab Học chia 2 nhóm "Từ vựng B2" / "Collocation TOEIC 🧩", cụm hiện thẻ C1..C14; trộn chữ chỉ ra với từ đơn (regex `/^[a-zA-Z]+$/` sẵn có).
- **`grammar.js`**: `window.GRAMMAR` = dữ liệu NGỮ PHÁP tĩnh, **viết tay & kiểm chuẩn** (KHÔNG sinh bằng AI — kiến thức phải đúng). **19 chủ điểm**: 12 thì (`group:present/past/future`, dùng `formula{aff,neg,ques}` → hiện +/−/?) + 7 cấu trúc (`conditional`×4 loại 0/1/2/3, `passive`, `reported`, `comparison` — dùng `forms:[{l,t}]` nhãn tự do → viewGrammar hiện "Cấu trúc"). Mỗi chủ điểm: `usage[]`, `signals[]`, `examples[{en,vi}]`, `practice[]` (type `conj` = chia động từ điền chỗ trống có `answer`+`alts`; type `mc` = trắc nghiệm `options`+`answer` index; reported dùng chủ yếu `mc`). Thêm chủ điểm mới chỉ cần append vào file (`GRAMMAR_GROUPS` tự hiện nhóm). File tĩnh, UTF-8, không nằm trong Supabase.
- **`emoji.js`**: `window.EMOJI_GROUPS` (9 nhóm × ~1914 emoji Unicode) + `window.EMOJI_VI` (từ khoá tiếng Việt) cho bộ chọn cảm xúc câu hỏi mỗi ngày. File tĩnh tách riêng như `library.js`, có trong SW cache.
- **`lottie/*.json`**: 3 animation TỰ TẠO (love/crown/rain) + lottie-web từ cdnjs.
- **PWA**: `manifest.json` + `sw.js` (service worker) + `icons/*`.
- **`schema.sql`**: toàn bộ bảng Supabase (chạy lại an toàn, idempotent).

### Config Supabase (đã điền sẵn trong index.html, ~dòng 220)
```
SUPABASE_URL = "https://vklsqexomaconbzoufmu.supabase.co"
SUPABASE_ANON_KEY = "sb_publishable_YDj6liDtYbMFq38GhwCwIQ_KlS-lNwI"   // publishable key MỚI, an toàn công khai
```
- Key mới dạng `sb_publishable_` (không phải `eyJ...` cũ) — supabase-js hiểu được. TUYỆT ĐỐI không commit `sb_secret_`.
- Bảng dùng RLS với policy "allow all" (using(true)/with check(true)) — bảo mật dựa vào **mã phòng khó đoán**, không phải RLS thật. Đủ cho web 2 người, đừng lưu thông tin nhạy cảm.

### Tầng dữ liệu `Store` (index.html)
Object `Store` có 2 chế độ: **cloud** (Supabase, khi có key) và **local** (localStorage, khi trống key — chỉ để demo). Mọi thao tác DB đi qua `Store.*` (getUser/createUser/updateUser, createInvite/listInvites/updateInvite, createSpace/getSpace/updateSpace, listDecks/saveDeck/deleteDeck, listCards/saveCard/saveCards/deleteCard, listQuizzes/saveQuiz, listNotes/saveNote/deleteNote, listDuels/saveDuel/updateDuel, getDaily/saveDaily). Poll mỗi 5s (`startPolling`) để đồng bộ; đấu trường poll nhanh 2.5s (`duelTick`).

### State chính (biến `S`)
`S = { user, space, me('a'|'b'), view, sub, decks, cards, quizzes, notes, dailyToday, invites, duel, duelPlay, duelHistory, play(study), compose, inboxTab, authTab, tmp }`. Người dùng là `a` hoặc `b` trong một `space` (phòng). `render()` đọc `S.view`.

### Bảng Supabase (schema.sql)
`users`(username pk, display, password_hash, space_id, person), `spaces`(id=mã phòng, person_a/b, person_a/b_username, theme, start_date, streak, longest, last_active), `invites`, `decks`, `cards`, `quizzes`(items jsonb), `notes`(+remind_at date, +remind_time, +done bool), `routines`(lịch sinh hoạt — xem mục Lịch), `daily`, `duels`(challenger, status, stake, questions jsonb, score_a/b, progress_a/b, done_a/b).
> ⚠️ Mỗi lần thêm cột/bảng, USER PHẢI tự chạy SQL trong Supabase SQL Editor (mình không có quyền). Luôn đưa đoạn SQL rõ ràng cho họ.

## Các màn/tab (nav dưới cùng): Nhà · Học · Đấu · Hộp thư · Lịch
- **Nhà (home)**: đếm ngày bên nhau, chuỗi/kỷ lục, nhiệm vụ hôm nay, câu hỏi mỗi ngày (thẻ bấm vào), banner nhắc/đấu/cài-app. Thẻ "ngày bên nhau" (`.couple`) là **ly chứa tim** (`HeartTank` — canvas `#heart-tank`, **55 tim** vật lý va chạm, đổ như nước, bán kính ~10.5–14.5px). Chống hỗn loạn: kẹp tốc độ tối đa (`MAXV`), va chạm mềm 2 lượt (nới 25%/lượt), tường triệt tiêu vận tốc đâm vào (không nảy), và cơ chế **"ngủ"** — tim đứng yên ≥16 frame thì đóng băng (`asleep`), chỉ THỨC khi nghiêng máy (đổi hướng trọng lực `gChange>0.04`) hoặc bị đẩy; nhờ đó cầm yên pile đứng im, nghiêng mới slosh (desktop không sensor thì luôn thức → đung đưa nhẹ). Nghiêng máy → lắc theo **`devicemotion.accelerationIncludingGravity`** (đọc thẳng vector trọng lực: `gx=-a.x`, `gy=+a.y` → tim luôn dồn đúng "đáy" thật; KHÔNG dùng `deviceorientation`/gamma vì công thức cũ sai làm phải lật ngược máy mới thấy tim). **Không có nút bật** — iOS tự xin `DeviceMotionEvent.requestPermission` ÂM THẦM ở lần `pointerdown` đầu tiên (`tryEnableHeartMotionOnce`); Android/desktop tự gắn trong `ensure()`; máy phẳng vẫn dồn nhẹ; không sensor → tự đung đưa (fallback khi `now-lastO>1500`). Canvas PHẢI có CSS `width/height:100%` (replaced element, nếu không sẽ hiện to hơn box → tim tràn/lọt); có kẹp biên cuối mỗi frame để tim không lọt ra. `render()` gọi `mountHeartTank()` khi ở home (giữ nguyên `parts` qua re-render), `HeartTank.stop()` khi rời/ẩn tab.
  - **Ảnh trong tim**: mỗi tim hiện ảnh (không phải màu hồng) nếu có `spaces.heart_photos` (mảng data URL, tối đa **`HEART_PHOTO_MAX`=55** ảnh, upload nhiều ảnh cùng lúc ở Cài đặt qua `addHeartPhotos`, nén 150px/JPEG q0.62 bằng `compressPhoto`); rỗng thì fallback về `avatar_a`/`avatar_b`; không có gì thì tim màu hồng như cũ. Mỗi ảnh được vẽ SẴN thành 1 "sprite" hình tim 1 lần (`HeartTank.makeSprite`, clip 1 lần) — mỗi frame chỉ `drawImage` lại, khỏi clip mỗi frame (đỡ nặng máy yếu). Vì `heart_photos` có thể nặng vài trăm KB (55 ảnh), poll 5s (`startPolling`) dùng `Store.getSpace(id, light=true)` bỏ cột này khỏi lần tải (chọn cột tường minh qua `SPACE_COLUMNS_LIGHT`, KHÔNG dùng `select('*')`) và giữ nguyên `heart_photos` cũ trong `S.space`; cứ mỗi ~60s (`pollTick%12===0`) mới tải đủ 1 lần để bắt kịp ảnh người ấy vừa up. Boot/login/pairJoin vẫn gọi `getSpace(id)` không tham số `light` → luôn tải đủ.
- **Học (library)**: "Bộ từ của mình" (tự tạo: thêm nhanh Enter/dán hàng loạt) + 14 chủ đề B2 có sẵn. Mỗi bộ: **Thẻ lật** (học nhớ) / **Trắc nghiệm** (thi, mix trắc nghiệm + trộn chữ) / Gửi / Rủ đấu. `startStudy(words,{mode:'flash'|'quiz',...})`, `buildQuestions()` dùng HẾT từ (không giới hạn 12). Có chip lọc **Ngữ pháp 📖** + section liệt kê chủ điểm `grammar.js` (nhóm theo `group`); bấm 1 chủ điểm → `S.sub='g:'+id` → `viewGrammar()` (công thức + cách dùng + dấu hiệu + ví dụ) → nút Luyện tập → `startGrammarPractice(id)` set `S.play.mode='grammar'` → `grammarPracticeView` (conj: input `#gr_input`+`check-grammar`; mc: `grammar-mc`; feedback `grFeedback` hiện đáp án + `explain`). `matchAnswer()` chuẩn hoá + quy đổi contractions 2 chiều (`don't`↔`do not`…) nên gõ dạng nào cũng đúng. `finishStudy` dùng chung (grammar `src!=='lesson'` → chỉ `bumpStreak`+`celebrate`).
- **Đấu (arena)**: "Rủ đấu" — chọn **Kiểu chơi** (Trắc nghiệm / Ô chữ 🧩 / Nối từ 🔗 — **Hangman 🎪 đang ẨN khỏi bộ chọn** theo yêu cầu user, code vẫn còn nguyên (`buildHangmanQuestions`/`duelHangmanBody`/nhánh `game==='hangman'` trong `createDuel`) chỉ bỏ nút trong `duelCreateView`, dễ bật lại nếu cần — đừng xoá code đó tưởng nhầm là dead code) + bộ + số câu + cược → người kia NHẬN LỜI (cược lại được, cột `stake_acceptor`) mới bắt đầu → cả hai làm **cùng 1 bộ câu hỏi** (sinh 1 lần lưu chung, mảng `questions` jsonb — KHÔNG có cột `game` riêng, kiểu chơi tự suy ra từ `questions[0].kind`) → thấy tiến độ đối thủ ~trực tiếp → xong hiện thắng(👑)/thua(🥈)/hòa(🤝) + cược + lịch sử. Chữ dùng "Rủ đấu" (KHÔNG dùng "thách đấu" — user thấy gắt). **Nhấn giữ 1 trận trong lịch sử** (`data-longpress="duel-stake"`) → popup `openDuelStakeModal` chi tiết ai cược gì + ai phải làm gì (winStake = `win===challenger?stake:stake_acceptor`, khớp `duelResultView`). Hangman/Ô chữ/Nối từ đều dùng chung `hangmanEligible()` để lọc "từ đơn" (chỉ chữ cái, không cụm — bộ collocation sẽ báo lỗi không tạo được trận).
  - **Hangman đoán từ 🎪** (`buildHangmanQuestions`/`hangmanEligible`/`duelHangmanBody`/`guessLetter`): mỗi câu là `{kind:'hangman',en,vi}` (từ đơn ≥3 chữ cái, lọc bằng `hangmanEligible` — bộ collocation toàn cụm sẽ báo "không có từ đơn" và không tạo trận được). **Mỗi người chơi ĐỘC LẬP cùng 1 từ** (không chung 1 hình, không luân phiên lượt) — giống hệt cơ chế mc/scramble hiện có, chỉ khác renderer, nên KHÔNG cần cột DB mới. Đoán chữ qua bàn phím A-Z (`d-guess-letter`); sai đủ `HANGMAN_MAX_WRONG=6` lần → thua từ đó (hiện đáp án), đoán đủ chữ trước đó → thắng (+1 điểm). Hình người vẽ dần bằng SVG (`hangmanSvg(wrong)`, 6 nét: đầu/thân/2 tay/2 chân). State đoán dở (`p.guessed` Set, `p.wrongCount`) chỉ ở client (`S.duelPlay`), KHÔNG persist per-letter lên DB — reset về từ đầu nếu thoát app giữa chừng 1 từ (chỉ `progress_a/b`+`score_a/b` mới lưu, giống các mode khác).
  - **Giải ô chữ 🧩** (`buildCrosswordQuestions`/`crosswordGridHTML`/`duelCrosswordPlayView`/`selectCrosswordCell`/`crosswordOnInput`/`crosswordSolve`/`finishCrosswordLocal`): dữ liệu layout (`{kind:'crossword',en,vi,row,col,dir,num}`, thuật toán tự xếp giao chữ cái) KHÔNG đổi. Nhưng **cách chơi đã đổi hẳn sang tap-to-select** (không còn "điền tuần tự theo thứ tự đánh số qua textbox" như bản đầu) theo yêu cầu user — vì giải **TỰ DO không theo thứ tự** (nhảy giữa các từ tuỳ ý), "tiến độ" không còn là 1 con số tuần tự (`p.idx`) mà là 1 TẬP các từ đã giải đúng, nên phải đổi cả cách lưu progress lên DB:
    - **Progress = bitmask** (`bitmaskToSet`/`setToBitmask`/`popcount`) nhét vào cột `progress_a`/`progress_b` (int có sẵn, KHÔNG cần cột mới) — bit thứ `i` bật = đã giải đúng `du.questions[i]`. Giới hạn an toàn ≤30 từ (dư so với UI 10/20/all). Khi hiện tiến độ đối thủ (progressBar, `duelTick`) phải `popcount()` ra SỐ LƯỢNG trước khi tính %, KHÔNG dùng trực tiếp giá trị cột như các mode khác.
    - **Gợi ý = chữ cái đầu mỗi từ luôn hiện sẵn trong ô bắt đầu** (`.cw-cell.hint`, không phải nghĩa tiếng Việt) — nghĩa tiếng Việt lùi thành gợi ý PHỤ, chỉ hiện trong thẻ clue bên dưới khi đã chạm chọn 1 từ. Chạm ô (`data-act="cw-cell"` → `selectCrosswordCell(row,col)`) → tìm các từ đi qua ô đó CHƯA giải (`crosswordWordsAt`), ô giao 2 từ thì bấm lần lượt **TOGGLE** giữa 2 từ.
    - **Gõ trực tiếp** vào `#cw_input` (không qua `handle()`/case chuẩn — bắt riêng ở `document.addEventListener('input')`) → `crosswordOnInput` chỉ **cập nhật DOM trực tiếp** các ô của từ đang chọn (KHÔNG gọi `render()` lại toàn view mỗi ký tự, để khỏi rớt focus/bàn phím ảo mobile) → đủ độ dài + khớp đáp án → `crosswordSolve()` (mới gọi `render()` đầy đủ, tự chuyển về "chưa chọn từ nào"). Sai đủ độ dài thì KHÔNG tự làm gì (không khoá, không xoá) — người chơi tự sửa lại, vẫn active từ đó.
    - Grid **full-width responsive** (`display:grid` + `aspect-ratio:1` mỗi ô, không còn 22px cố định) để lấp đầy màn hình bất kể bao nhiêu cột.
    - Không có "hoàn thành tự động theo thứ tự" nữa — nút **"Nộp bài 🏁"** (`finishCrosswordLocal`) để người chơi tự dừng dù chưa giải hết (tính điểm theo số từ đã giải), hoặc tự động `doneLocal` khi giải đúng HẾT các từ.
  - **Nối từ 🔗** (`isChainDuel`/`chainTurn`/`sendChainWord`/`validateChainWord`/`duelChainBody`) — DUY NHẤT trong 3 mode mới là **luân phiên lượt thật** (không phải "độc lập cùng đề" như quiz/Hangman/ô chữ), nên có nhánh render RIÊNG trong `duelPlayView` (`if(isChainDuel(du)) return duelChainBody(du)`, bỏ qua hẳn cơ chế `p.idx`/`progressBar`). Tạo trận: random 1 từ đơn từ bộ đã chọn làm "từ mở đầu" lưu ngay vào `questions=[{kind:'chain',word,by:challenger,at}]` (KHÔNG dùng "Số câu" — ẩn hẳn UI đó cho mode này) → người rủ đã "dùng lượt" nên tới lượt người nhận lời nối tiếp. Lượt hiện tại suy từ `chain.length%2` (không lưu cột riêng): lẻ→người nhận lời, chẵn→người rủ (`chainTurn`). Từ nối validate 2 lớp: (1) `validateChainWord` (đồng bộ, tức thì) — chữ cái đầu = chữ cuối từ trước + không lặp từ + chỉ chữ cái; (2) `dictWordExists(word)` (bất đồng bộ, gọi **dictionaryapi.dev** — API từ điển miễn phí, có CORS, KHÔNG cần key, gọi thẳng từ trình duyệt không qua Cloudflare Function) — xác minh từ có THẬT trong tiếng Anh không (đã tự kiểm chứng qua `preview_eval`: ~750-820ms/lần, trả 404 đúng cho chuỗi vô nghĩa). Sai 1 trong 2 lớp → toast báo lỗi, KHÔNG tính thua, vẫn còn giờ để gõ lại. Trong lúc tra từ điển: khoá `#dchain_input` + nút đổi text "Đang tra từ điển…" (biến `p.chainSending` chặn gửi trùng). Nếu API lỗi/mất mạng (`dictWordExists` trả `null`) → **tạm chấp nhận** từ đó (không chặn ván chơi vì lỗi mạng, khớp triết lý toàn app), chỉ báo toast nhẹ. ⚠️ Lưu ý: dictionaryapi.dev dùng nguồn Wiktionary nên vài từ ngắn/hiếm tưởng vô nghĩa (vd "te" = nốt nhạc solfège) vẫn có thể qua được vì đúng là từ thật trong từ điển đó — không phải bug.
    - **Đếm ngược `CHAIN_TURN_SEC`=15s/lượt tính từ lúc THỰC SỰ THẤY màn hình, KHÔNG phải lúc đối phương gửi xong** — bug đã sửa: bản đầu dùng `at` (thời điểm gửi) làm mốc, nên animation đấm kiếm (1.5s) + poll chưa kịp phát hiện + mở app trễ đều bị trừ thẳng vào 10s của người chơi (user report: mở app lên chỉ còn 2s). Giờ dùng `seenAt` — timestamp riêng ghi vào ĐÚNG phần tử cuối của `chain` (`chainLastAt`/`chainRemainSec` trả `null` nếu chưa có `seenAt` = "lượt này chưa ai xem, chưa bắt đầu đếm"), do `ensureChainSeen(du)` set NGAY (optimistic, trước cả khi lưu DB) khi `duelChainBody` render và đúng lượt mình (`chainTurn(du)===S.me`) — CHỈ chính người tới lượt mới set được giờ của họ, người đang chờ xem màn hình KHÔNG được tự set hộ. Nhờ vậy đồng hồ luôn hiện đủ 15s thật từ đúng lúc người chơi mở màn hình lên, bất kể animation/độ trễ mạng trước đó. `startChainTimer()`/`stopChainTimer()` chạy `setInterval` 1s RIÊNG (không phải `render()` lại toàn bộ mỗi giây — chỉ update trực tiếp `#chain-countdown`/class `.warn` qua DOM, tránh mất giá trị đang gõ dở của `#dchain_input`; hiện `—` khi `remain===null`), bật/tắt trong `render()` (`if(S.view==='arena'&&S.duel?.status==='active'&&isChainDuel(S.duel)) startChainTimer(); else stopChainTimer()`), và GUARD không gọi `handleChainTimeout()` khi `remain===null` (chưa ai xem thì không thể tính hết giờ). Hết giờ → `handleChainTimeout()`: người TỚI LƯỢT thua (`score_<họ>=0`, đối thủ `=1`), `status:'done'` — **BẤT KỲ máy nào** (kể cả máy không tới lượt) phát hiện trước cũng tự chốt được, ghi cùng 1 kết quả nên 2 máy cùng phát hiện vẫn an toàn (không cần lock/transaction).
    - ⚠️ **`CHAIN_GRACE_SEC`=5s — khoảng đệm SAU khi hết 15s mới thực sự tính thua** (điều kiện dùng `remainRaw` THÔ chưa `Math.max(0,...)`: `if(remainRaw<=-CHAIN_GRACE_SEC) handleChainTimeout()`). Lý do: `sendChainWord()` phải `await dictWordExists()` (gọi mạng, có thể mất tới ~4s do AbortController timeout trong đó) TRƯỚC KHI từ được coi là hợp lệ — nếu gửi từ sát giây cuối, `startChainTimer()` (chạy độc lập trên CẢ 2 máy, kể cả máy đối phương đang chờ) vẫn tiếp tục đếm trong lúc chờ API trả lời, và nếu không có khoảng đệm sẽ báo thua NGAY GIỮA LÚC đang kiểm tra từ điển dù người chơi đã bấm gửi kịp giờ (user report: "gửi ở giây thứ 1 thì bị reject báo thua luôn"). Countdown hiển thị vẫn dừng ở "0s" bình thường khi hết 15s danh nghĩa, chỉ riêng THỜI ĐIỂM THỰC SỰ ghi thua bị lùi thêm 5s để đủ chờ API. Đã tự kiểm chứng qua `preview_eval`: gửi ở giây thứ 1 kèm giả lập API delay 3s → không bị báo thua giữa chừng, từ vẫn được thêm vào chuỗi bình thường; ngược lại nếu thực sự không làm gì quá 20s (15+5) thì vẫn bị tính thua đúng.
    - **Animation đấm kiếm chỉ phát 1 LẦN DUY NHẤT/trận** (không chỉ riêng Nối từ — áp dụng mọi mode đấu): trước đây `S.tmp.duelClashDone` chỉ lưu trong bộ nhớ (Set), nên tải lại trang/mở lại app giữa ván lại phát animation (1.5s) một lần nữa — với Nối từ (trận có thể kéo dài nhiều lượt qua nhiều lần mở app) điều này góp phần ăn giờ oan. Giờ lưu thêm `localStorage loveapp_duel_clash_seen` (`clashSeenIds`/`markClashSeen`, cùng pattern với `duelSeenIds`/`markDuelSeen`) nên chỉ phát đúng 1 lần cho cả đời trận đấu.
    - ⚠️ **GOTCHA đã vá**: `duelTick()` (poll 2.5s) TRƯỚC ĐÂY chỉ re-render khi `status`/`done_x` đổi — với nối từ, từ MỚI của đối thủ chỉ đổi độ dài `questions`, KHÔNG đổi `status`/`done_x`, nên máy bên kia sẽ KHÔNG THẤY từ mới / KHÔNG đổi lượt nếu không vá thêm điều kiện `chainChanged` (so độ dài `questions` trước/sau) vào biến `changed`. Nhớ giữ điều kiện này nếu sửa `duelTick`.
    - Test bằng `preview_eval`: mọi lệnh set-state-rồi-render phải gói trong **1 lệnh eval duy nhất** — nếu tách rời nhiều lệnh, cả `chainTimer` (1s) lẫn `duelTick`/`startPolling` polling THẬT (gọi `Store.listDuels` tới Supabase thật, ghi đè `S.duel`) đều có thể tự kích hoạt/xoá state giữa các lệnh do độ trễ thực giữa các lần gọi tool.
- **Hộp thư (inbox)**: bài dò nhận/gửi. Bài **từ vựng** có Thẻ lật/Trắc nghiệm; bài **ngữ pháp** (gửi từ `viewGrammar` qua `sendGrammar`, `items` = câu luyện có `.type`) nhận diện bằng `isGrammarQuiz(q)` → hiện 1 nút "Luyện tập" (`study-lesson` → `startGrammarPractice(null,{quiz})`), làm xong `finishStudy` đánh dấu answered + điểm (mode `grammar` cũng được tính như `quiz`). KHÔNG cần cột DB mới (dùng lại bảng `quizzes`, items jsonb).
- **Lịch (`S.view==='notes'`, `viewSchedule`)**: ⚠️ tab này TRƯỚC ĐÂY tên là "Nhắc" — user yêu cầu nâng cấp thành **LỊCH SINH HOẠT SONG SONG của hai đứa**, nên KEY view vẫn giữ `'notes'` (đỡ phải sửa `NAV_ORDER`/back-handling/badge) chỉ đổi nhãn + icon `cal`. Chia 4 khu bằng `.seg` (`S.tmp.schTab`): **`day`** lịch ngày · **`mon`** lịch tháng (`schMonthView` — lưới T2→CN, chấm hồng=mình bận/nâu=người ấy bận/🔔=có nhắc, **ngày ÂM LỊCH dd/mm chữ nhỏ dưới ngày dương** (mùng 1 tô màu `--accent`), bấm ô ngày → `sch-pick` vào lịch ngày; lùi/tới tháng `sch-mon`, `S.tmp.schMonth`) · **`rt`** thói quen · **`note`** = màn Nhắc CŨ nguyên vẹn (`notesBody()`, tách ra từ `viewNotes` cũ).
  - **Bảng `routines`** (MỚI, phải chạy SQL): `person` `'a'|'b'|'both'`, `start_min`/`end_min` (phút từ 0h), `freq` `daily|weekly|once` + `days` `'1,3,5'` (0=CN) hoặc `date`, `emoji`, `color`, `done_days` jsonb (mảng 'YYYY-MM-DD'). Bảng chưa tồn tại thì `loadRoutines()` **nuốt lỗi** và bật cờ `routinesMissing` → tab Lịch hiện hướng dẫn chạy SQL, KHÔNG làm chết `loadAll()` (nếu để nó ném thì hỏng luôn màn Nhà).
  - **Lịch ngày (`schDayView`)**: 1 trục giờ ở giữa, việc CỦA MÌNH cột trái, việc NGƯỜI ẤY cột phải. **LUÔN vẽ đủ 0h–24h trong `.tl-scroll` (max-height 62vh, cuộn trong khung, THANH CUỘN BỊ GIẤU** — `scrollbar-width:none` + `::-webkit-scrollbar{display:none}` vì user chê "trông rất thô"; thay bằng 2 vệt mờ `.tl-fade.t/.b` trong `.tl-wrap` để vẫn gợi ý "còn nữa") — bug cũ: khung chỉ vẽ 7h–22h nên lời nhắc 22:06 rơi ra ngoài không thấy. `mountSchScroll()` (gọi cuối `render()`) lần đầu tự cuộn tới "bây giờ"/việc sớm nhất, các render sau giữ nguyên chỗ đang xem qua `S.tmp.schScrollTop` (lưu bởi listener scroll capture, reset khi vào tab/đổi ngày/lưu việc). MỌI lời nhắc của ngày (kể cả KHÔNG hẹn giờ — loại này không ghim được lên trục) hiện thêm thành dải chip `.sch-nchip` trên đầu lịch. `person:'both'` render ở **cả hai cột** (cùng `data-id`, nên đừng dùng `querySelector` đơn khi thao tác DOM). Khối đặt `position:absolute` theo `SCH_HOUR_PX=58` px/giờ; khung giờ vẽ ra do `schRange()` quyết (tối thiểu 7h–22h, tự nới theo việc thật). `schLanes()` chia làn khi 2 việc CÙNG CỘT chồng giờ. `freeBands()` = phần bù của hợp các khoảng bận trong 7h–23h, ≥45 phút mới hiện dải "cùng rảnh 💞". `schLinks()` đặt tim ở giữa khi việc chung hoặc **trùng tên + trùng giờ ≥15 phút** (so tên đã `noDiacritics` + gộp khoảng trắng). Lời nhắc `notes` có `remind_time` của đúng ngày đó được **ghim vào cột giữa** (`.tl-pin`).
  - **NHẬP LIỆU = KÉO DỌC trên cột của mình** (user chọn cách này): kéo → ô mờ `.tl-ghost` xem trước khung giờ (làm tròn 15 phút), thả tay mới mở popup đặt tên; bấm/chạm nhẹ ô trống (không di chuyển >7px) = thêm nhanh 1 tiếng. **CHUỘT vs CHẠM khác nhau** (user report bản đầu "kéo 1 lượt không ăn" + long-press web bôi chọn text hiện menu copy): `pointerType==='mouse'` → vào chế độ kéo NGAY từ pointerdown + `e.preventDefault()` (chuột không cuộn bằng kéo nên không cần chờ; chờ giữ là lý do kéo-liền-một-mạch bị coi là cuộn rồi huỷ); chạm → giữ **180ms** (lúc đó trình duyệt chưa bắt đầu cuộn) rồi mới `preventDefault` trên `touchmove` **non-passive** — nhúc nhích >14px trước khi đủ giữ = đang cuộn trang, huỷ không giành. `.tl` đặt `user-select:none` + `-webkit-touch-callout:none` để long-press không bôi chọn text/hiện menu copy. `startPolling` bỏ qua vòng poll khi `SchDrag.on` để khỏi `render()` mất ô mờ đang kéo.
  - **Popup thêm/sửa (`rtSheetHTML`)**: 12 chip gợi ý một chạm (`RT_SUGGEST` — điền luôn emoji + tên + khung giờ hợp lý), ai làm (mình / cả hai / người ấy), lặp lại (mỗi ngày / theo thứ / một lần), màu, ghi chú. Popup tự vẽ lại khi đổi lựa chọn → **luôn gọi `rtSync()` trước `rtRefresh()`** kẻo mất chữ đang gõ dở. `<input type=time>` không nhận `24:00` nên hiển thị kẹp về `23:59`.
  - **Bấm một việc** (`openRoutineSheet`): ✓ xong (theo TỪNG NGÀY, lưu vào `done_days`, giữ 60 ngày gần nhất — KHÔNG cộng streak vì streak dành cho học/thi) · **💌 nhắc người ấy** (`schNudge`) chỉ hiện với việc của người kia / việc chung · sửa/xoá chỉ với việc của mình.
  - ⚠️ **"Nhắc nhau" KHÔNG có bảng mới**: `schNudge` tạo luôn một `notes` với `remind_at`=hôm nay + `remind_time`=**giờ hiện tại** → đến hạn ngay → đi qua đúng đường **push-worker/vps-push** sẵn có. Đừng dựng cơ chế nudge riêng.
  - **ÂM LỊCH (`solarToLunar`/`lunarLabel`)**: thuật toán **Hồ Ngọc Đức** (tính ngày Sóc + kinh độ Mặt Trời, `LUNAR_TZ=7`) — tự tính, KHÔNG bảng tra nên chạy mọi năm; trả `{d,m,y,leap}`, `lunarLabel` ra `'15/8'` (tháng nhuận thêm `+`). Đã kiểm bằng mốc thật: Tết 2023–2027, Rằm tháng Giêng, Giỗ tổ 10/3, Trung thu 15/8, Đoan Ngọ 5/5, nhuận tháng 2 (2023) & tháng 6 (2025), 01/01/2000 = 25/11 âm; mọi tháng âm đều 29 hoặc 30 ngày. ⚠️ Trước khi sửa "bug" âm lịch nhớ kiểm bằng nguồn thật: **Tết 2026 KHÔNG có ngày 30 tháng Chạp** (tháng đó chỉ 29 ngày) — đúng chứ không sai.
  - **ĐƯA LỊCH VÀO ĐIỆN THOẠI — file `.ics` (2026-07)**: nút "Vào máy" cạnh tiêu đề tab Lịch → `openIcsModal`/`icsSheetHTML`/`icsUrl` (index.html) + **`functions/api/ics.js`** (Pages Function, `GET /api/ics?room=<mã phòng>&who=a|b|all[&alarm=5|0|off]`). Điện thoại **đăng ký một lần** (webcal:// trên iOS, calendar.google.com trên Android) rồi **app Lịch của MÁY tự báo thức** — không cần Web Push, **iPhone không phải cài PWA vào màn hình chính** (gỡ được điểm yếu lớn nhất của push).
    - **KHÔNG thay thế Web Push, chỉ chia việc**: lịch đăng ký được máy tải lại theo chu kỳ CỦA NÓ (iPhone ~15 phút–1 tiếng tuỳ cài đặt, Google Calendar có khi vài tiếng) → chỉ hợp với việc ĐẶT TRƯỚC. Việc cần báo NGAY (`schNudge` nhắc nhau, tới lượt nối từ) vẫn phải đi đường push-worker/vps-push. Đừng gỡ push đi tưởng ICS làm thay được.
    - Function đọc thẳng Supabase REST (hằng `SUPA_URL`/`SUPA_KEY` trong file, dùng chung publishable key). Bảo mật = **mã phòng khó đoán**, sai mã trả **404** như các endpoint lab. Bảng `routines` chưa tồn tại → `catch(()=>[])`, vẫn xuất được lời nhắc.
    - `routines` → VEVENT lặp: `daily`→`RRULE:FREQ=DAILY`, `weekly`→`FREQ=WEEKLY;BYDAY=` (⚠️ **DTSTART phải rơi đúng một trong các thứ đã chọn** — code tự đẩy ngày gốc tới ngày hợp lệ gần nhất), `once`→ không RRULE. `notes` có `remind_at` → VEVENT một lần (có `remind_time` = đúng giờ, không có = cả ngày + báo lúc 8h). Bỏ note `done`. `person:'both'` vào lịch của cả hai; `who=all` gắn thêm tên người kia.
    - ⚠️ Ba chỗ dễ sai đã vá & có test khoá: (a) **gấp dòng RFC 5545 phải đếm theo BYTE** (tiếng Việt 2 byte/ký tự, emoji 4) và không được cắt giữa ký tự → hàm `fold` duyệt từng ký tự; (b) `end_min=1440` (24:00) phải **tràn sang 00:00 ngày hôm sau**, `end<=start` tự nới 30 phút; (c) việc lặp tạo từ lâu bị **kẹp `PAST_DAYS`=60 ngày** kẻo DTSTART kéo lịch sử nhiều năm. Có `VTIMEZONE` Asia/Ho_Chi_Minh (+07 cố định, VN không có giờ mùa hè).
    - `icsUrl()` dùng `location.origin`, nhưng preview local (`localhost`) không có `/api` nên tự quay về hằng `ICS_ORIGIN='https://cungnhau.pages.dev'`.
    - Test: `node` với `globalThis.fetch` giả (44 test cho Function: lọc theo người, RRULE, tràn giờ, escape, gấp dòng ≤75 octet, alarm, thiếu bảng) + bóc riêng khối ICS trong index.html chạy với stub (18 test cho UI). Đã kiểm trên domain thật bằng `curl` với phòng thật.
  - Test: `preview_eval` hoặc chạy Playwright (firefox) với `Store` giả trong bộ nhớ — đã kiểm cả kéo thả thật bằng `mouse.down()` + chờ 320ms + `mouse.move()`.
- **Cài đặt (⚙️)**: mã phòng, mời theo tên, ngày bắt đầu yêu, tên, tông màu, đăng xuất/rời phòng, hướng dẫn cài app.

Streak (`bumpStreak`): học/thi/trả lời câu hỏi xong → chuỗi +1 (1 lần/ngày, no-op nếu đã +hôm nay).

### Câu hỏi mỗi ngày — react & nhắn lại kiểu Messenger (index.html: `dAnswerCard`, `viewDaily`)
- Mỗi câu trả lời là 1 "comment": thẻ của mình chỉ có nút **Sửa**; thẻ người ấy có thanh hành động **🙂 Cảm xúc · 💬 Trả lời** (kiểu FB Thích|Trả lời).
- **Bộ chọn cảm xúc đầy đủ (emoji picker) kiểu Messenger** (`openEmojiPicker(who)`, bottom-sheet `.emoji-sheet` dùng lại `.overlay`/`.sheet`): mở bằng nút "Cảm xúc" (`d-react-open`) HOẶC long-press 480ms (`data-longpress="daily-react"` → cũng gọi `openEmojiPicker`). Có ô tìm kiếm (`#emoji_q`, gõ tiếng Anh hoặc **tiếng Việt không dấu** — "tim/cười/hoa"; khớp `it[1]` tên EN + `EMOJI_VI[emoji]`, `noDiacritics`), hàng "hay dùng" (recent lưu `localStorage loveapp_emoji_recent` + `REACT_DEFAULTS`, `emojiQuickList`), tab nhóm (`emoji-cat` → `scrollIntoView`), lưới `.emoji-grid` chia 9 nhóm (`emojiSectionsHTML`) / kết quả phẳng khi tìm (`emojiSearchHTML`, cap 120). Chọn emoji (`emoji-pick`, who lấy từ `S.tmp.emojiFor`) → `closeModal()` + `dailyReact` (đẩy vào recent). Bấm lại emoji đang chọn = bỏ (toggle, `dailyReact` trả null). Sheet có `data-act="stop"` (no-op) để click trong sheet không đóng; backdrop `.overlay` `close-modal` để đóng.
- **Bộ emoji = `emoji.js`** (`window.EMOJI_GROUPS` = 9 nhóm × ~1914 emoji Unicode, nguồn `unicode-emoji-json`; `window.EMOJI_VI` = ~108 từ khoá tiếng Việt cho emoji hay dùng). File tĩnh tách riêng như `library.js`, nhúng qua `<script src="emoji.js">`, **có trong SW SHELL cache** (`sw.js`, nhớ tăng `CACHE` version khi đổi).
- **Ô trả lời viên thuốc** (`.reply-pill` + `.reply-send` icon send): mở bằng nút "Trả lời" (`d-reply-toggle`), Enter hoặc nút gửi để lưu (`d-reply-send`). Hiện lại dưới dạng bong bóng comment (`.reply-quote`).
- State ẩn ở `S.tmp.emojiFor` (who đang thả cảm xúc) + `S.tmp.dReplyOpen` (who đang mở ô trả lời, reset khi `go`). Cột DB: `daily.reaction_a/b`, `reply_a/b` (a/b = câu trả lời CỦA AI được react/nhắn). `saveDailySafe()` fallback bỏ cột mới nếu Supabase chưa chạy SQL (báo toast), không chặn app.
- **Nhật ký ngày trước** (`dailyJournal`, `dayJournalCard`, `journalAnswer`): mỗi ngày đã là 1 dòng `daily` riêng → `Store.listDaily(sid)` (order day desc) nạp qua `loadDailyHistory()` vào `S.dailyHistory`, CHỈ khi `go` tới `daily` (không nằm trong poll 5s để đỡ nặng). View lọc bỏ hôm nay + ngày trống, hiện read-only kèm reaction/reply + avatar. `S.dailyHistory===null` = đang tải.

### Ảnh đại diện (index.html: `avatarEl`, `compressAndSaveAvatar`)
- Lưu **data URL JPEG** trong `spaces.avatar_a/b` (không dùng Supabase Storage). Upload: `chooseAvatar()` mở input file → vẽ canvas **128×128 cover-crop** → `toDataURL('image/jpeg',0.72)` (~1.5KB) → `updateSpace`. Catch lỗi nếu chưa chạy SQL cột avatar.
- `avatarEl(person,size,ring)`: có ảnh → `<div class="ava"><img>`, chưa có → chữ cái đầu tên trên gradient. Dùng ở: Cài đặt (đổi/bỏ ảnh của `S.me`), màn Nhà (2 avatar chồng nhẹ, `ring`), câu hỏi mỗi ngày (bong bóng reply + ô nhập). `.ava` cũ letter-only đã nâng cấp (thêm `img`, `.ring`).

### Tìm kiếm & lọc tab Học (index.html)
- 28 chủ đề (14 vựng + 14 colloc) nên có ô tìm kiếm (`data-filter="lib"`, bỏ dấu qua `noDiacritics()`, gõ "hop dong" vẫn ra "Hợp đồng") + chip lọc nhanh Tất cả/Của mình/Từ vựng/Collocation (`S.tmp.libFilter`).
- `applyLibFilter()` chạy lại sau mỗi `render()` khi ở `view==='library'` không mở topic con, ẩn cả `.lib-sec` rỗng và hiện `#lib-empty` khi không khớp gì.

## AI — Cloudflare Workers AI qua Pages Functions (KHÔNG phải server Node riêng)
- Thư mục **`functions/api/*.js`** = Cloudflare **Pages Functions**, tự thành endpoint trên cùng domain `cungnhau.pages.dev`. `wrangler.toml` khai báo binding `[ai] binding="AI"` → hàm gọi `env.AI.run(MODEL, {...})`. Deploy vẫn `wrangler pages deploy .` (thấy dòng "Uploading Functions bundle").
- 2 endpoint: **`/api/daily-question`** (POST `{recent:[]}` → `{question}`) và **`/api/word-example`** (POST `{en,vi}` → `{example,example_vi,usage}`).
- index.html gọi qua `apiPost(path,body,ms)` (có timeout, ném lỗi để fallback). **Câu hỏi mỗi ngày**: `ensureDaily` thử AI trước, lỗi/chế độ máy → dùng `DAILY_QUESTIONS` cố định (sinh 1 lần/ngày, lưu chung `daily.question`). **Ví dụ từ**: nút "Xin ví dụ (AI)" ở màn thẻ lật (`flashExample`, `aiExampleForCurrent`, cache theo từ trong `S.play.exCache`).
- ⚠️ **Model Workers AI hay bị deprecate** (báo lỗi `5028`). Dùng `npx wrangler ai models` để xem model còn sống rồi đổi hằng `MODEL` trong 2 file. Hiện dùng `@cf/meta/llama-3.3-70b-instruct-fp8-fast`.
- ⚠️ `env.AI.run` có khi trả `response` là **object** (không phải string) → luôn `String(out.response)` / kiểm tra `typeof` trước khi `.trim()`/parse. Endpoint tự parse JSON lỏng lẻo (regex `\{...\}`).
- Preview local (`python http.server`) KHÔNG có `/api` → `apiPost` 404 → fallback êm; muốn test AI thật phải deploy rồi `curl` domain thật.

## Chuyển động & màn chào (index.html)
- **Màn chào = `viewAuth`** (trước khi đăng nhập) đóng vai landing page: khối logo `.land .mark` (tay-trong-tay, tự nhấp nhô `markFloat` 5s), tiêu đề, 1 câu giới thiệu, 3 viên `.land-feat` (Học từ vựng / Rủ nhau đấu / Giữ chuỗi) rồi mới tới form. Các khối trồi lên lần lượt nhờ class **`.stagger`** trên `.screen` (con thứ 1..5 có `animation-delay` tăng dần, dùng `@keyframes rise`) — dùng lại được cho màn khác chỉ bằng cách thêm class.
- **`@media (prefers-reduced-motion:reduce)` toàn cục** ép mọi animation/transition về ~0ms. Đặt CUỐI phần `<style>` để thắng các rule trước. KHÔNG có chỗ nào nghe `animationend`/`transitionend` nên tắt animation không làm kẹt luồng nào (đã kiểm bằng grep) — nếu sau này thêm listener như vậy phải xem lại.

## AI đọc tài liệu — trang ẨN `ai-lab-124.html` + `functions/api/lab/*`
- **Tính năng ẨN theo yêu cầu user**: KHÔNG link từ index.html, KHÔNG trong SW cache, có `noindex`. Vào bằng cách gõ đúng URL `cungnhau.pages.dev/ai-lab-124.html`. API `/api/lab/extract` + `/api/lab/analyze` yêu cầu header `x-lab-key` = hằng `LAB_KEY='cn-lab-124-tim'` (khai báo ở CẢ 3 file, phải khớp nhau) — sai key trả **404** giả vờ không tồn tại (cùng triết lý "mã khó đoán" như mã phòng).
- **Pipeline client-side** (ai-lab-124.html tự điều phối, mỗi bước 1 request để né giới hạn CPU/time của Functions): (1) Đọc tài liệu — dán chữ trực tiếp; `.txt/.md/.csv/.html` đọc bằng FileReader; PDF/ảnh POST bytes thô lên `/api/lab/extract` (ảnh nén canvas ≤1600px JPEG trước) → (2) `cleanText` + `chunkText` chia đoạn 6k ký tự gối đầu 250, cắt trần 60k → (3) 1 đoạn thì task `summary_full`; nhiều đoạn thì map/reduce: `summary_chunk` từng phần → `summary_merge` → (4) task `questions` (tóm tắt + `excerptsOf` trích đoạn gốc đầu+giữa ~11k) → bộ câu hỏi.
- **Giao diện đã thiết kế lại (2026-07)** cho khớp app chính, **KHÔNG dùng emoji** — icon SVG nét mảnh nội bộ (hằng `ICONS` + `ic(name)` trong trang, `[data-ic]` trong HTML tự thay bằng SVG khi tải). Nền giấy kẻ ô + viền đậm + bóng cứng như index.html. Khu hero đầu trang, các thẻ trồi lên lần lượt (`.rise`/`.d1..d4`), có `prefers-reduced-motion`.
- **Tuỳ chọn khi ra đề** (gửi kèm task `questions`): số câu (5/8/12/15), **`difficulty`** easy|medium|hard, **`qtype`** mc|tf|mix (tf = Đúng–Sai, `options` chỉ 2 mục — `normQuestion` sẵn có đã chấp nhận ≥2 lựa chọn nên KHÔNG cần đổi DB/validate), **`focus`** (ô chữ tự do "muốn tập trung phần nào", ≤300 ký tự, chèn vào cuối user prompt). Bỏ trống mọi tuỳ chọn = hành vi y như cũ (mc/medium/không focus).
  - ⚠️ **Gợi ý độ khó phải tách riêng cho `tf`**: bản đầu dùng chung một bộ, câu "lựa chọn gây nhiễu sát nghĩa" của mức hard MÂU THUẪN với Đúng–Sai (chỉ 2 mục cố định) → model bỏ luôn yêu cầu tf, trả về 4 lựa chọn. Đã kiểm chứng trên domain thật trước/sau khi sửa.
- **Thêm ở client**: thanh tiến độ % tổng (mỗi bước chiếm 1 dải trong hằng `PHASES`) + trạng thái từng bước; chọn nguồn Dán chữ/Tệp bằng `.seg`; đếm ký tự khi dán; **thẻ ghi nhớ lật** từ danh sách `terms` (chỉ hiện khi có ≥2 từ khoá có nghĩa); chép tóm tắt; tải kết quả `.md`; vòng tròn điểm số vẽ dần (stroke-dashoffset); **"Làm lại câu sai"** ngoài "Làm lại tất cả". `shuffle()` KHÔNG xáo options của câu 2 lựa chọn (Đúng/Sai đảo chỗ sẽ vô nghĩa), câu 4 lựa chọn vẫn xáo và giữ đúng index đáp án.
- **`/api/lab/analyze`**: 4 task (`summary_full`/`summary_chunk`/`summary_merge`/`questions`), model chính **`@cf/openai/gpt-oss-120b`** (mạnh nhất Workers AI hiện tại, dùng **Responses API**: input `{instructions,input}`, output `{output:[{type:'message',content:[{text}]}]}` — KHÔNG phải messages/response như llama) → lỗi hoặc JSON hỏng thì tự **fallback `@cf/meta/llama-3.3-70b-instruct-fp8-fast`** (messages API). `extractText()` hiểu mọi dạng output, `looseJson()` bóc JSON khỏi ```json fence/văn xuôi. Câu hỏi được `normQuestion` lọc/ép kiểu (answer chuỗi → số, câu thiếu options bị bỏ).
- **TÌM TÀI LIỆU BẰNG AI (2026-07)** — tab đầu tiên khu Nguồn dữ liệu. Người dùng nói tự do muốn học gì ("tôi muốn học ngân hàng thương mại, mới bắt đầu") → **`/api/lab/search`** chạy **3 chặng**, mỗi chặng 1 request để client hiện tiến trình:
  1. **`plan`** (có AI) — hiểu yêu cầu, đoán trình độ, chia 3-6 mục con theo thứ tự học, soạn 3-5 **truy vấn tìm kiếm** (vi/en theo chip chọn).
  2. **`find`** (KHÔNG AI, không tốn token) — chạy các truy vấn qua **DuckDuckGo Lite**.
  3. **`curate`** (có AI) — chọn ≤12 tài liệu, xếp **thứ tự nên đọc**, kèm lý do + trình độ + loại tài liệu + gợi ý lộ trình.
- ⚠️ **TUYỆT ĐỐI KHÔNG để AI tự đọc ra đường dẫn.** Model Workers AI không truy cập internet → hỏi link là nó bịa. **Đã đo thật**: hỏi 8 link tài liệu về "ngân hàng thương mại" chỉ **2/8 còn sống**, 6 cái 404/403 — tên miền thật (imf.org, fdic.gov, bankofengland.co.uk) nhưng đường dẫn bịa. Sau khi đổi sang kiến trúc 3 chặng: **10/10 link sống**. **Chốt an toàn bằng cấu trúc**: chặng `curate` bắt AI trả về **SỐ THỨ TỰ** trong danh sách kết quả thật, không cho trả URL — nó không bịa link được kể cả khi muốn (cùng cách `synthesis` dùng chỉ số nguồn). `validCurate` loại chỉ số ngoài phạm vi + trùng. Có test khoá lại: nhét pick chứa URL bịa rồi khẳng định kết quả trả về **không chứa chuỗi "http" nào**.
- ⚠️ **Ba cái bẫy đã gặp và vá khi làm crawl tìm kiếm — đừng lặp lại:**
  - **Quảng cáo DuckDuckGo bọc HAI LỚP**: `uddg=` lại trỏ về chính `duckduckgo.com/y.js?ad_domain=…`. Bóc một lớp rồi trả luôn là link quảng cáo lọt thẳng vào danh sách tài liệu. `unwrapDdg` phải kiểm tra lại host SAU khi bóc (`isRealTarget`).
  - **Mojeek là đường lui GIẢ**: chọn nó vì probe thấy HTTP 200, nhưng nội dung là trang **Captcha**. Đo bằng mã HTTP thôi chưa đủ — phải nhìn nội dung. Đã bỏ hẳn; thay bằng chạy truy vấn **tuần tự có nghỉ 350ms + thử lại 1 lần** khi một truy vấn ra rỗng (bắn 4-5 truy vấn song song từ cùng IP bị DDG chặn tạm, cả mẻ về 0 kết quả).
  - **Tìm ra link đẹp mà không đọc được thì vô dụng**: scribd trả trang "Client Challenge" (227 ký tự, **lọt ngưỡng 200 ký tự cũ** của fetch.js), slideshare/studylib không ra chữ. Trước khi lọc, **3/5 tài liệu AI xếp đầu đều thuộc nhóm này**. Đã (a) thêm `skipSite()` loại thẳng nhóm trang xem tài liệu có tường phí/chặn bot ở chặng `find`, (b) fetch.js thêm regex `CHALLENGE` bắt trang chặn bot + nâng ngưỡng lên 300 ký tự, (c) nhắc trong prompt `curate` ưu tiên trang đọc được trực tiếp. Sau khi vá, kết quả toàn PDF giáo trình trường đại học và tạp chí ngành.
  - ⚠️ `SKIP_NAME` (tên miền cấp 2, ghép thêm `\.[a-z.]+$`) phải TÁCH khỏi `SKIP_FULL` (tên miền đủ đuôi như `academia.edu`, `tailieu.vn`) — nhét chung sẽ thành "academia.edu.xxx" và không khớp gì cả.
- **BẢN ĐỒ Ý CHÍNH "Cần nắm gì & nằm ở đâu" (2026-07)** — thẻ `#keycard` sau thẻ tóm tắt (user chê tóm tắt suông "chưa mang ý nghĩa"). Client `buildSegments(text)` chia tài liệu thành ĐOẠN đánh số (cắt theo dòng trống; đoạn >700 ký tự cắt tại ranh giới câu ~550, KHÔNG dùng regex lookbehind vì Safari cũ) → task **`keypoints`** trong analyze.js nhận `{title, segs:[{t}]}` (mỗi đoạn chỉ gửi phần đầu, `per=clamp(60, 16000/n, 240)` ký tự) → trả `{goal, keys:[{t,why,from,to}]}`. **`from`/`to` là SỐ ĐOẠN** — cùng chốt chống bịa như curate/synthesis: `validKeypoints` loại ý có `from` ngoài 1..n, `to<from` co về `from`, phạm vi >12 đoạn bị cắt (`to=from+11`). Giao diện trỏ thẳng `s.segs[from-1..to-1]` nên trích đoạn LUÔN là chữ thật. Luồng bấm: chạm 1 ý (`data-kp`) → popup sheet `openKeyPoint` (why + đúng các đoạn đó + nút) → `data-reader` → **trang đọc toàn bài** `#reader` (`openReader`/`readerJump`: cả bài theo đoạn đánh số, chip nhảy nhanh `#rd-jumps`, đoạn đích tô sáng `.rd-seg.mark`, cuộn tới giữa màn; đóng = `#rd-close`/Esc, nhớ trả `document.body.style.overflow`). Nút `data-readall` mở đọc toàn bài không cần qua popup; bấm `<a>` trong keymap được `closest('a')` bỏ qua như ở `#results`. Chạy TRONG bước map (mỗi nguồn +1 lượt AI sau tóm tắt), lỗi thì `s.keys=null` — không chết cả mẻ. Lịch sử không giữ toàn văn → mỗi ý lưu kèm `ex` (~400 ký tự) trong `lab_runs.sources[].keys` để `openRun` xem lại được (không có trang đọc trong lịch sử); export .md cũng in kèm (`pushKeys`).
- **HIỆN TRÊN BẢN PDF GỐC (2026-07)** — user chê đọc trên chữ markdown "tracking rất khó đọc", nên PDF giờ hiện **đúng bản gốc** chứ không phải chữ đã bóc:
  - **`PDF.js 3.11.174` (cdnjs, bản classic script — 4.x là ESM nhúng `<script>` thường không được)** nạp LƯỜI qua `loadScript` lần đầu cần tới, `GlobalWorkerOptions.workerSrc` trỏ file worker cùng phiên bản. `readPdfBlob(blob)` trả `{doc, pages:[{n,w,h,text,items}], text}`; `items` = mỗi mẩu chữ `{o,n,x,y,w,h}` — `o/n` là VỊ TRÍ KÝ TỰ trong `page.text`, `x/y/w/h` là toạ độ **ở scale 1** (lúc vẽ mới nhân theo cỡ màn hình → xoay/đổi cỡ máy vẫn tô đúng chỗ). Tổng chữ <200 = PDF scan không có lớp chữ → trả `null` để lùi về đường cũ (`/api/lab/extract` + Workers AI). Trần `PDF_MAX_PAGES`=300.
  - **`/api/lab/raw`** (MỚI): POST `{url}` → nguyên bytes PDF. Cần vì trình duyệt tải PDF khác tên miền bị CORS chặn. Dùng lại `safeUrl`+`readCapped` của fetch.js (đã `export` thêm `readCapped`), trần 14MB, và **chỉ trả khi thật sự là PDF** (`looksPdf`: chữ ký `%PDF-` hoặc content-type pdf) + `nosniff` — cố ý KHÔNG để thành proxy vạn năng. Sai `x-lab-key` → 404 như các endpoint lab khác.
  - **`readSource` ưu tiên PDF.js**: tệp `.pdf` tải lên → `readPdfBlob`; đường dẫn khớp `isPdfUrl` → `fetchPdfDoc` (qua `/api/lab/raw`); `/api/lab/fetch` trả `kind:'pdf'` (URL không có đuôi .pdf) → cũng thử lấy bản gốc. Thất bại ở bất kỳ bước nào đều lùi êm về cách đọc cũ.
  - **`segmentPositions(text)`** thay lõi của `buildSegments` — trả thêm khoảng ký tự `{t,a,b}`. PDF dùng `segmentsFromPdf(pdf)` chia đoạn **theo TỪNG TRANG** → `s.segs` (chuỗi, để AI đánh số như cũ) + `s.segLoc` (song song, `{p,a,b}`). Nhờ vậy `keypoints` không phải đổi gì: AI vẫn chỉ trả số đoạn.
  - **Trang đọc 2 chế độ**: `mountPdfPages` (canvas PDF thật) / `mountTextPages` (chữ đánh số, cho web & chữ dán). Trang PDF dựng khung sẵn bằng `aspect-ratio` rồi **chỉ vẽ khi sắp lọt tầm nhìn** (`IntersectionObserver`, rootMargin 600px) nên PDF vài trăm trang mở được ngay; canvas nhân `devicePixelRatio` (cap 2). `pdfJump` quy khoảng ký tự → `rectsForRange` (cắt theo tỉ lệ ký tự khi giao một phần) → `mergeRects` (gộp mẩu cùng dòng) → thẻ `.pdf-hl` đặt bằng **phần trăm khổ trang** (`mix-blend-mode:multiply` để không che chữ). Đóng trang đọc thì `RD.io.disconnect()`; chạy mẻ mới thì `doc.destroy()`.
  - Nhãn vị trí: **`locLabel(s,k)`** → "trang 12" khi có `segLoc`, ngược lại `segLabel(k)` → "đoạn 3–5". Lịch sử/`.md` lưu sẵn chuỗi `k.loc` (vì lịch sử không giữ `segLoc`), đọc lại bằng `k.loc || segLabel(k)`.
- ⚠️ **RA ĐỀ LÀ NÚT BẤM RIÊNG, KHÔNG tự chạy** (yêu cầu user: sinh đề mỗi lần phân tích rất tốn token). `run()` dừng ở bước nối nguồn rồi chỉ lưu ngữ cảnh vào biến **`ANALYSIS`** `{summaryTxt, excerpts}`; **`generateQuestions()`** (nút `#gen-quiz` trong thẻ Kiểm tra hiểu bài) mới gọi task `questions`. Các tuỳ chọn số câu/độ khó/kiểu/tập trung đã CHUYỂN từ thẻ nguồn xuống thẻ này. "Làm lại tất cả"/"Làm lại câu sai" chỉ xáo đề cũ (0 token); "Soạn đề khác" mới gọi AI lần nữa. Bản ghi `lab_runs` lưu ngay lúc phân tích xong với `questions:[]`, bấm tạo đề thì **`updateRun`** vá `questions`+`model_q` vào đúng bản ghi đó (lịch sử hiện "chưa tạo đề" nếu chưa bấm). `STEP_DEFS` còn 4 bước, `PHASES` chia lại cho phủ kín 0→100% — nếu thêm/bớt bước nhớ chỉnh cả hai cho khớp (test tự kiểm điều này).
- **NHIỀU NGUỒN CÙNG LÚC (2026-07)** — trang giờ nhận **danh sách nguồn** trộn lẫn: dán nhiều **đường dẫn** (mỗi dòng một cái), nhiều **tệp**, nhiều **đoạn chữ**; tối đa `MAX_SOURCES`=10 (kể cả trang con), `MAX_PER_SOURCE`=40k ký tự/nguồn, `MAX_TOTAL`=120k cả mẻ. Mảng `SOURCES` giữ trạng thái riêng từng nguồn (`idle/reading/ok/err`) — **một nguồn lỗi KHÔNG làm chết cả mẻ**, chỉ hiện lỗi ở dòng đó rồi chạy tiếp; hết sạch nguồn đọc được mới ném lỗi. Luồng: đọc từng nguồn → `summarizeOne` tóm tắt RIÊNG từng nguồn (1 phần gọi `summary_full`, nhiều phần thì map/reduce) → **`synthesis`** nối lại (chỉ khi ≥2 nguồn) → `questions` ra đề trên toàn bộ (dùng `excerptsOfAll` chia đều hạn mức 12k cho các nguồn, có nhãn `[NGUỒN i]`). Kết quả hiện 3 thẻ: Tổng hợp chung · **Các nguồn liên quan nhau thế nào** (chủ đề chung + mối liên hệ từng cặp) · Từng nguồn nói gì (`<details>` mở sẵn cái đầu).
- **`/api/lab/fetch`** (MỚI): POST `{url}` → `{title, siteName, desc, text, links[], kind}`. Tải trang PHÍA MÁY CHỦ nên không dính CORS. ⚠️ **KHÔNG dùng được Scrapling** (github.com/d4vinci/Scrapling) như user đề xuất — đó là thư viện **Python**, Workers runtime là JS, không có pip. Nên phần bóc chữ tự viết bằng xử lý chuỗi (Workers không có DOM): bỏ `script/style/nav/header/footer/aside/form`, ưu tiên vùng `<article>`/`<main>` (chọn khớp DÀI NHẤT), đổi thẻ khối thành xuống dòng, giải mã ký tự HTML, **bỏ dòng ngắn (<60 ký tự) bị lặp** (menu/nút). Hệ quả: **không chạy JS của trang đích** → trang SPA trả **422** kèm lời khuyên lưu PDF rồi tải lên; muốn render JS thật thì phải chạy trình thu thập riêng trên VPS (xem `vps-push/`), Workers không làm được. PDF ở cuối URL vẫn đọc được qua `env.AI.toMarkdown`. Có **rào chặn SSRF** (`safeUrl` — chặn localhost/127./10./192.168./172.16-31./169.254./`.local`/`.internal`/fc00::, chỉ http-https), trần 4MB (`readCapped` đọc theo stream rồi cắt), timeout 20s.
  - **Gợi ý "đọc thêm trang con"** (chip 0/2/4 trang): `collectLinks` chấm điểm liên kết (cùng tên miền, chữ neo dài, đường dẫn sâu; TRỪ điểm cho nút điều hướng, `/tag/ /category/`, và đường dẫn có dấu `:` = trang phụ trợ wiki như `Wikipedia:`/`Special:`). ⚠️ **Phải thu thập liên kết TRONG vùng nội dung đã lọc, không phải cả trang** — Wikipedia không bọc menu trong `<nav>` nên quét cả trang thì "Nội dung chọn lọc", "Trang chủ"… đè hết bài viết thật (đã gặp và sửa; vùng nội dung <3 liên kết mới lùi về quét toàn trang). Trang con được thêm vào `SOURCES` với `parent` = id nguồn cha, hiện thụt vào (`.src.child`), và bị xoá đi ở đầu mỗi lần chạy mới.
- **Task `synthesis`** trong analyze.js: nhận `sources:[{label,tldr,points}]` (2..12) → `{tldr, points[], themes:[{title,detail,sources[]}], connections:[{from,to,kind,note}], terms[]}`. `kind` là một trong *bổ sung / trùng ý / khác biệt / mâu thuẫn / nhân quả*. `validSynthesis` **loại chỉ số nguồn ngoài 1..n và cặp tự nối chính mình** (model hay bịa ra nguồn thứ 3 khi chỉ có 2) — giao diện trỏ thẳng vào `list[i-1]` nên chỉ số sai sẽ vỡ. Dưới 2 nguồn trả 400 (client tự bỏ qua bước này, dùng luôn tóm tắt của nguồn duy nhất).
- ⚠️ **Cột mới `lab_runs.sources` + `lab_runs.synthesis`** (jsonb) — USER PHẢI chạy SQL. Chưa chạy thì `saveRun` tự **thử lại bản rút gọn** (bỏ 2 khoá đó) và `loadHistory` tự lùi về danh sách cột cũ (`COLS_LITE`), không chặn tính năng.
- **`/api/lab/extract`**: ảnh → OCR bằng `@cf/meta/llama-3.2-11b-vision-instruct` (prompt transcribe, `image:[...bytes]`), lỗi/rỗng → fallback `env.AI.toMarkdown`; PDF/khác → `env.AI.toMarkdown({name,blob})` (Workers AI Markdown Conversion, trả `{data}` hoặc mảng). Tối đa 10MB, không đọc được chữ → 422.
- Đã test bằng node + mock `env.AI` (cả 2 dạng output model, fallback, sai key, lọc câu hỏng, chunk/overlap/shuffle) — xem cách làm nếu sửa.
- **LƯU để so sánh/đánh giá**: mỗi lần phân tích xong, trang nhúng `@supabase/supabase-js@2` (dùng chung URL+anon key) tự `insert` vào bảng **`lab_runs`** (schema.sql, RLS allow-all): source/file_name/char_count/chunks, `model`+`model_q` (endpoint `/analyze` giờ trả kèm `model` = model THẬT đã tạo ra kết quả — main hay fallback), input_excerpt (3000 ký tự đầu), tldr/points/terms/questions (jsonb), ms. Khu **"Lịch sử & đánh giá 🗂️"** ở cuối trang: liệt kê 40 lần gần nhất, **chấm sao 1-5** (`rateRun`, bấm lại sao đang có = bỏ) + **ghi nhận xét** (`noteRun`, cột `rating`/`note` điền sau) + "Xem lại" (bottom-sheet `.overlay/.sheet` hiện lại tóm tắt + câu hỏi kèm đáp án đúng) + Xoá. Bảng `lab_runs` chưa tồn tại → khu này hiện hướng dẫn chạy SQL, KHÔNG chặn phân tích (saveRun bắt lỗi, toast nhẹ). ⚠️ USER phải tự chạy SQL bảng `lab_runs` trong Supabase.

## Lottie (index.html: `mountLotties`, `initLoading`)
- lottie-web cdnjs 5.12.2. 3 file trong `/lottie/`: **love** (tim đập, loading overlay `#loading`), **crown** (vương miện nảy, khi THẮNG), **rain** (mây+mưa, khi THUA).
- `mountLotties()` chạy cuối `render()`, quét `[data-lottie]:not([data-lottie-done])`. Thắng→confetti, thua→KHÔNG confetti (`celebrate` gated `iWin||tie`).
- ⚠️ **GOTCHA**: repeater Lottie LỒNG NHAU (dọc×ngang) bị nhân sai offset trong lottie-web → rain.json dùng **mỗi cột 1 repeater dọc riêng** (sinh bằng Python, 10 cột so le). Đừng lồng repeater.

## Thông báo đẩy (Web Push) — `push-worker/`
- Nhắc (notes) có `remind_at`(ngày) + `remind_time`(giờ 'HH:MM', không bắt buộc) + `notified_at` (đã đẩy push chưa, chặn gửi lại). Nút "Bật thông báo đẩy" ở tab Nhắc (`enableNotif` trong index.html) xin quyền Notification + đăng ký `PushManager.subscribe` bằng `VAPID_PUBLIC_KEY` (hardcode ở index.html, không nhạy cảm) rồi lưu endpoint/p256dh/auth vào bảng `push_subscriptions` (Supabase). Nếu subscribe lỗi (không hỗ trợ, chưa cài app trên iOS…) vẫn fallback về thông báo "chỉ khi mở app" cũ (`maybeNotify`), không chặn app.
- **`push-worker/`** là 1 **Cloudflare Worker RIÊNG** (không phải Pages Functions của app chính) — chạy cron `* * * * *` = **mỗi 1 phút** (`push-worker/wrangler.toml`; Cloudflare cron là best-effort, có thể lệch 1-2 phút; muốn đúng-giây phải chuyển sang Durable Object Alarm hẹn từng lời nhắc), tự kiểm tra `notes` tới giờ (so `remind_at`+`remind_time` theo giờ VN UTC+7, hardcode `TZ_OFFSET_HOURS=7`) rồi đẩy Web Push tới mọi `push_subscriptions` cùng `space_id` (cả hai người, giống cách `maybeNotify` cũ báo chung cho cả phòng). Gửi xong (hoặc không có ai để gửi) thì set `notified_at` — mỗi lời nhắc chỉ đẩy **1 lần**.
- **`vps-push/`** = **giải pháp thay thế chạy trên VPS người dùng** (vì Cloudflare cron trễ ~10-13 phút). Node 18+, dùng thư viện **`web-push`** (không phải tự mã hoá như push-worker) + Supabase REST, **poll mỗi 20s** (systemd, tự restart). Logic `dueAtMs`/`notified_at` giống push-worker → cột `notified_at` chặn gửi trùng nên chạy song song vẫn an toàn (nhưng README khuyên tắt cron Cloudflare sau khi VPS ổn). Dùng LẠI đúng cặp VAPID cũ: `convert-key.js` chuyển khoá riêng PKCS8 (đang ở Cloudflare) → dạng web-push (raw 32-byte urlsafe, = `jwk.d`) nên KHÔNG phải đổi `VAPID_PUBLIC_KEY` trong index.html, KHÔNG phải đăng ký lại. Đã test round-trip khoá + `generateRequestDetails` bằng web-push thật. Chỉ gọi ra ngoài, không mở cổng vào VPS.
- Tự cài đặt mã hoá **RFC 8291 (aes128gcm) + VAPID (RFC 8292)** bằng Web Crypto (`crypto.subtle`) thuần, KHÔNG dùng thư viện `web-push` (Workers runtime không cần npm install gì thêm). Đã tự kiểm chứng round-trip (mã hoá rồi giải mã lại, ký JWT rồi verify) bằng `preview_eval` trước khi tin dùng — xem lại nếu cần sửa thuật toán, đừng đoán mò vì sai sẽ SILENT FAIL (không báo lỗi rõ ràng, chỉ đơn giản là không có thông báo tới máy).
- **Deploy TÁCH RIÊNG** khỏi app chính (khác lệnh `wrangler pages deploy`):
  ```
  cd push-worker
  wrangler secret put VAPID_PRIVATE_KEY_PKCS8   # dán khoá riêng tư PKCS8 base64 (KHÔNG commit vào repo)
  wrangler deploy
  ```
- ⚠️ Mỗi lần thêm cột/bảng liên quan (`push_subscriptions`, `notes.remind_time`, `notes.notified_at`) → USER PHẢI tự chạy SQL (xem schema.sql phần "Bổ sung cột").
- ⚠️ iOS Safari: chỉ nhận được push nếu đã **cài app vào màn hình chính** (PWA installed), mở tab thường không đủ quyền push.

## Triển khai (deploy)
- **Chính: Cloudflare Pages** → **https://cungnhau.pages.dev** . Lệnh (đã đăng nhập wrangler):
  ```
  npx wrangler pages deploy . --project-name=cungnhau --branch=main --commit-dirty=true
  ```
- **Phụ: GitHub Pages** → https://johnong55.github.io/BeTogether/ (repo PUBLIC). Tự deploy khi `git push`, NHƯNG hay bị nghẽn hàng đợi "deployment_queued" → dùng Cloudflare là chính.
- Quy trình: sửa → `git add/commit/push` → `wrangler pages deploy`. `.wrangler/` đã gitignore (chỉ chứa account id, không secret).
- ⚠️ Cloudflare redirect `/index.html`→`/` (308). `manifest.json start_url` và `sw.js` phải trỏ `./` (KHÔNG `./index.html`). SW dùng **network-first**, tăng `CACHE` version mỗi lần đổi shell.

## Trang quản trị hệ thống — `admin.html` (TÁCH RIÊNG khỏi app chính)
- File **`admin.html`** độc lập (KHÔNG nằm trong `index.html`, KHÔNG trong SW cache, có `<meta robots noindex>`), truy cập qua `cungnhau.pages.dev/admin.html` hoặc `johnong55.github.io/BeTogether/admin.html` (GitHub Pages tự deploy khi merge vào `main`). Dùng CHUNG `SUPABASE_URL`/`ANON_KEY` và **cùng hàm `hashPass` djb2** với index.html. Vì RLS "allow all" + anon key nên admin đọc/ghi thẳng MỌI bảng từ trình duyệt (không có backend riêng, đúng triết lý app).
- **Giao diện = DASHBOARD sidebar** (không phải tabs pill như bản đầu): layout `.shell` = grid `sidebar 248px + main`; sidebar `.side` (brand + `.nav` các `.item` data-act="nav", footer hiện admin+vai trò), main = `.topbar` (tiêu đề trang + ↻ + Đăng xuất) + `.content#page`. **Mobile ≤860px**: sidebar off-canvas (`transform:translateX(-102%)`), mở bằng nút `.burger` (`data-act="toggle-nav"` → toggle `body.nav-open`), có `.scrim` backdrop; chọn 1 mục nav tự đóng drawer (`A.navOpen=false`). Mảng `NAV` (có `{sep}` chia nhóm, `{owner:true}` chỉ owner thấy) sinh menu; `pageTitle`/`pageSub` map tiêu đề. State điều hướng ở `A.tab`+`A.navOpen`.
- **Bảng `admins`** (username pk, passcode băm djb2, **role**, created_at) — đã thêm vào `schema.sql` + vòng RLS. Vào lần đầu (bảng rỗng) admin.html tự hiện màn **"Thiết lập quản trị"** để tạo tài khoản đầu tiên (self-bootstrap, tài khoản đầu LUÔN là `owner`). Bảng `admins` chưa tồn tại → hiện màn hướng dẫn chạy `schema.sql`. Phiên đăng nhập lưu `sessionStorage` (`cungnhau_admin_session`).
- **Phân quyền theo vai trò (cột `admins.role`)**: `owner` (Chủ hệ thống — toàn quyền + quản lý admin khác) / `manager` (Quản lý — sửa/xóa user & phòng, KHÔNG thấy tab admin) / `viewer` (Chỉ xem — mọi nút sửa/xóa bị ẩn). Helper `canEdit()`/`canManageAdmins()` + guard `requireEdit()`/`requireOwner()` gate CẢ nút (ẩn trong render) LẪN hàm hành động (chặn khi gọi). Tab **🛡️ Quản trị viên** (chỉ owner): thêm admin (tên+mật khẩu+vai trò), đổi vai trò qua `<select class="rolesel">` (bắt bằng `change` listener), xóa admin. **Bất biến an toàn: luôn còn ≥1 owner** (không cho xóa/hạ cấp owner cuối, không cho tự xóa mình) — kiểm ở cả UI (disable/ẩn nút) lẫn hàm. `roleOf(a)` fallback `'owner'` khi thiếu cột (DB cũ) để không khóa nhầm.
- Các mục: **Tổng quan** (stat cards nhóm Người dùng&phòng / Nội dung / Đang diễn ra + phòng hoạt động gần đây) · **Người dùng** (tìm kiếm, Đặt lại PIN qua prompt→`hashPass`, Gỡ ghép = null `space_id`/`person` + gỡ `person_x_username` trong space, Xóa user) · **Phòng** (tìm kiếm, bấm dòng → modal chi tiết: sửa nhanh name/start_date/streak/theme, liệt kê decks/quizzes/notes/duels/daily, **Xóa phòng** = xóa `spaces` cho FK `on delete cascade` dọn decks/cards/quizzes/notes/duels/daily/push_subscriptions, còn users+invites tự dọn tay).
- **Thư viện chung toàn hệ thống** (mục 📚, bảng `library_topics` + `library_words` mới trong schema.sql, RLS allow-all): kho chủ đề/bộ từ DÙNG CHUNG (dự kiến mọi cặp đôi học được — **chưa wiring vào index.html/Học**, đó là bước sau). `loadLibrary()` tải RIÊNG (try/catch — bảng chưa tồn tại thì mục hiện hướng dẫn chạy SQL, KHÔNG làm hỏng dashboard). `library_topics`: title, `kind`(vocab/colloc/grammar), `source`, `status`(draft/published), created_by. Tạo chủ đề (`openNewTopicModal`→`createTopic`, có dán hàng loạt `parseBulk` "từ = nghĩa = ví dụ"), mở chủ đề (`openTopicModal`) sửa meta + thêm/xóa từ (`addWord`/`bulkWord`/`delWord`), `delTopic` cascade. Mọi mutation gate `requireEdit()`.
- **Nguồn tri thức** (mục 🧲, `viewIngest`): KHUNG nhập tri thức → đích là Thư viện chung. Thẻ **Crawl** (input disabled, badge "Sắp có" — cần Cloudflare Function proxy tải trang tránh CORS) + **OCR PDF** (dropzone disabled, badge "Sắp có" — dự kiến Tesseract.js chạy client) đều CHƯA làm (user chốt "làm sau"); thẻ **Nhập thủ công** DÙNG ĐƯỢC (nút → `openNewTopicModal`). Khi làm crawl/OCR thật thì cho ghi vào `library_topics`/`library_words` (status có thể để 'draft' chờ duyệt).
- `loadAll()` fetch song song mọi bảng (cột tường minh, KHÔNG lấy avatar/heart_photos nặng). Event delegation `data-act` giống app chính (dùng `data-act="stop"` cho click trong modal, `overlay`/`close-modal` để đóng). Đã tự test bằng Playwright + mock Supabase in-memory (setup/login/dashboard/search/sửa phòng) — network CDN bị chặn trong sandbox là bình thường.

## Cách test (QUAN TRỌNG)
- Preview: `.claude/launch.json` chạy `python -m http.server 4599`. Dùng `preview_start`/`preview_eval`.
- ⚠️ **`preview_screenshot` HAY TREO** khi trang có animation chạy liên tục (lottie/confetti) — kể cả sau `Lot.freeze()`. **Xác minh bằng DOM** qua `preview_eval` (đếm số phần tử SVG, đọc textContent) thay vì ảnh.
- Test không đụng DB thật: giả lập bằng cách gán `S.user/S.space/S.me/S.view` và **stub** `Store.*`, `bumpStreak`, `ensureDaily`, `maybeNotify` bằng bộ nhớ tạm rồi gọi `render()`/`handle(act,v,id,el)`.
- ⚠️ `render()` kiểm tra `S.view==='auth'` TRƯỚC → khi test phải set `S.view` đúng, không thì thấy màn đăng nhập.
- lottie load JSON bất đồng bộ → sau `render()` phải chờ ~500ms mới thấy `<svg>`.

## Sự cố đã xử lý (đừng lặp lại)
- Nút trong popup không chạy: do `onclick="event.stopPropagation()"` chặn event delegation → đã thay bằng `data-act="stop"` + case no-op. Nếu "nút bấm không gọi API", nghi ngờ delegation bị chặn.
- Local mode (không key) khác cloud mode — mọi hàm Store phải làm được cả hai.

## Đặc điểm người dùng
Không rành kỹ thuật. Muốn thấy kết quả chạy thật. Thích chữ nhẹ nhàng ("Rủ đấu", "Biết rồi 💕" thay vì câu gượng). Test trên iPhone (Safari) và Android. Luôn deploy Cloudflare + đưa link + hướng dẫn Ctrl+Shift+R / cài lại app sau khi đổi.
